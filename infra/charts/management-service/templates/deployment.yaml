apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "xp.fullname" . }}
  labels:
    {{- include "xp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.xpApi.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "xp.name" .}}
      release: {{ .Release.Name }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: {{ template "xp.name" .}}
        release: {{ .Release.Name }}
        {{- include "xp.labels" . | nindent 8 }}
    spec:
      {{- with .Values.xpApi.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "xp.serviceAccountName" . }}
      containers:
      - name: api
        image: "{{ .Values.xpApi.image.registry }}{{ .Values.xpApi.image.repository }}:{{ .Values.xpApi.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.xpApi.image.pullPolicy }}
        env:
          {{- with .Values.xpApi.extraEnvs }}
          {{- toYaml . | nindent 12 }}
          {{- end }}
        ports:
          - name: http
            containerPort: 8080
            protocol: TCP
        livenessProbe:
          httpGet:
            path: {{ .Values.xpApi.livenessProbe.path }}
            port: {{ .Values.xpApi.service.internalPort }}
            scheme: HTTP
          initialDelaySeconds: {{ .Values.xpApi.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.xpApi.livenessProbe.periodSeconds }}
          successThreshold: {{ .Values.xpApi.livenessProbe.successThreshold }}
          timeoutSeconds: {{ .Values.xpApi.livenessProbe.timeoutSeconds }}
        readinessProbe:
          httpGet:
            path: {{ .Values.xpApi.readinessProbe.path }}
            port: {{ .Values.xpApi.service.internalPort }}
            scheme: HTTP
          initialDelaySeconds: {{ .Values.xpApi.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.xpApi.readinessProbe.periodSeconds }}
          successThreshold: {{ .Values.xpApi.readinessProbe.successThreshold }}
          timeoutSeconds: {{ .Values.xpApi.readinessProbe.timeoutSeconds }}
        resources:
          {{- toYaml .Values.xpApi.resources | nindent 12 }}
        # Give time for running pods to terminate existing connection before letting
        # Kubernetes terminate the pods.
        # https://blog.sebastian-daschner.com/entries/zero-downtime-updates-kubernetes
        lifecycle:
          preStop:
            exec:
              command: ["/bin/bash", "-c", "sleep 15"]
        args:
        - serve
        - --config=/etc/xp/config.yaml
        {{- if .Values.xpApi.uiConfig }}
        - -ui-config
        - /etc/xp/ui.config.json
        {{- end }}
        {{- with .Values.xpApi.extraArgs }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        volumeMounts:
        - name: config
          mountPath: /etc/xp
        {{- with .Values.xpApi.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      - name: swagger-ui
        image: "swaggerapi/swagger-ui:{{ .Values.swaggerUi.image.tag }}"
        imagePullPolicy: IfNotPresent
        ports:
          - containerPort: {{ .Values.swaggerUi.service.internalPort }}
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 128Mi
        env:
        - name: LAYOUT
          value: "BaseLayout"
        - name: SWAGGER_JSON
          value: "/app/experiments.yaml"
        - name: PORT
          value: "{{ .Values.swaggerUi.service.internalPort }}"
        - name: API_SERVER
          value: "{{ .Values.swaggerUi.apiServer }}"
        command: ["sh", "-c"]
        args:
        - |
          mkdir /app
          export LOCAL_API_SERVER="http://127.0.0.1:{{ .Values.xpApi.service.internalPort }}"
          echo "Fetching swagger configuration from ${LOCAL_API_SERVER}/experiments.yaml..."
          until $$(wget -O $${SWAGGER_JSON} --tries 1 --timeout 1 ${LOCAL_API_SERVER}/experiments.yaml); do
            printf '.'
            sleep 10
          done
          echo "Fetching swagger configuration from ${LOCAL_API_SERVER}/schema.yaml..."
          until $$(wget -O /app/schema.yaml --tries 1 --timeout 1 ${LOCAL_API_SERVER}/schema.yaml); do
            printf '.'
            sleep 10
          done
          echo "Update Swagger config..."
          sed -r -i 's%^((\s*)-(\s*)url\s*:).*$$%\1 "'$${API_SERVER}'"%' $${SWAGGER_JSON}
          echo "Running Swagger UI..."
          /usr/share/nginx/run.sh
      volumes:
      - name: config 
        secret:
          secretName: {{ template "xp.fullname" .}}-api-config
      {{- with .Values.xpApi.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
